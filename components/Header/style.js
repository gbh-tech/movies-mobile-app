import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
    headerTitle: {
        width: 200,
        color: '#fff',
        fontSize: 26,
        paddingLeft: 10
    }
})

export default style;